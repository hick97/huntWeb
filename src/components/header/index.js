import React from 'react';
import './style.css';


// modelo alternativo a classe que é comument utilizado para componentização (recomendada para quando o conceito de estado é utilzado)
const Header = () => (
    <header id='main-header'>JShunt</header>
);

export default Header;